# Fotos

Pon aqui las fotos que quieras usar en el regalo.

Cuando esten listas, se puede generar la red fija con:

```bash
node tools/build-memories.mjs
```

El preparador intenta leer primero el ano EXIF de cada JPEG. Si no existe, usa la fecha de creacion que macOS tenga para el archivo.
